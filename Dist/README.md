# Distribution Outputs

This folder is for generated release artifacts such as local macOS installer packages.

Generated files are intentionally ignored by git. Recreate the current macOS installer with:

```sh
Scripts/package-macos-installer.sh
```
